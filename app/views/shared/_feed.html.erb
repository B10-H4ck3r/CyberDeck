<% if @feed_items.any? %>
  <ol class="microposts feed_txt">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items,
                    params: { controller: :users, action: :show } %>

<% end %>
